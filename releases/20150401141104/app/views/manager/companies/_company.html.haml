%tr{:class => 'company'}
  %td #{company.id}
  %td
    %strong Name: #{company.name}
    %br
    Description: #{company.description}
  %td= link_to('Show', manager_company_path(company), {:class => 'btn btn-small btn-success btn-block'})